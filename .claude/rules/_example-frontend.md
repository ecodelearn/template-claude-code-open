---
paths:
  - "src/**/*.tsx"
  - "src/**/*.jsx"
  - "src/**/*.css"
  - "src/**/*.scss"
---

> **TEMPLATE** — customize for your project or delete if not applicable.
> This rule loads automatically only when Claude works with files matching the paths above.
> Rename to `frontend.md` and remove this notice when ready.

# Frontend conventions — [project-name]

<!-- Add your frontend-specific rules here. Examples: -->

<!-- ## Components
- Naming: PascalCase for components, camelCase for hooks
- One component per file
- Export as named export, not default

## Styling
- Use [CSS Modules / Tailwind / styled-components] — never inline styles
- Mobile-first breakpoints

## State management
- Local state: useState/useReducer
- Global state: [Zustand / Redux / Context]
- Server state: [React Query / SWR]

## Testing
- Co-located tests: ComponentName.test.tsx
- Test behavior, not implementation -->
